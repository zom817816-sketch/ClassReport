# 课情报告生成器 - 教师版

## 首次配置（只需一次）

1. 双击 `Configure_Feishu.bat`，它会自动用记事本打开 `.env`。
2. 填写认证方式：推荐在 `USER_ACCESS_TOKEN=` 后粘贴管理员提供的用户访问令牌；也可填写 `APP_ID` 与 `APP_SECRET`，以应用身份访问。
3. 如需切换到另一份多维表格，在 `FEISHU_APP_TOKEN=` 后填入其 app_token（完整 Base 链接中的那段字符串）并保存。
4. 双击 `ClassReportGenerator.exe`。

## 日常使用

双击 `ClassReportGenerator.exe`。程序会下载最新飞书数据、生成个人 PDF；完成后在同目录的 `output\packages` 查找班级压缩包。首次启动需要几秒钟解压运行组件。

在“报告模板”中可选择“标准专业风”“手绘卡通风”“孟菲斯几何风”或“校园笔记风”；选择会保存在 EXE 同目录的 `ClassReportGenerator.config.env`，下一次启动会自动沿用。

## 注意事项

- 用户访问令牌可能会过期；出现鉴权错误时，向管理员索取新的令牌并替换 `.env` 中的值。
- 不要通过微信、群聊或邮件公开发送 `.env`，其中含有访问凭据。
- 每位学生 PDF 的密码为其手机号后四位；请仅在获得授权的范围内分发。
