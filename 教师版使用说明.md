# 课情报告生成器 - 教师版

## 首次配置（只需一次）

1. 将 `teacher_config.env.example` 重命名为 `.env`。
2. 用记事本打开 `.env`，在 `USER_ACCESS_TOKEN=` 后粘贴管理员提供的飞书用户访问令牌并保存。
3. 双击 `一键生成课情报告.bat`。

## 日常使用

双击 `一键生成课情报告.bat`。程序会下载最新飞书数据、生成个人 PDF，并自动打开 `output\packages` 中的班级压缩包。

## 注意事项

- 用户访问令牌可能会过期；出现鉴权错误时，向管理员索取新的令牌并替换 `.env` 中的值。
- 不要通过微信、群聊或邮件公开发送 `.env`，其中含有访问凭据。
- 每位学生 PDF 的密码为其手机号后四位；请仅在获得授权的范围内分发。
